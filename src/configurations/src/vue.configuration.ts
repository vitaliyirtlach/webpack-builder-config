import { PackageJSONOptions } from "../../shared/interfaces/PackageJSON.options"

export const vueConfiguration = async ({appName, appDescription}: PackageJSONOptions): Promise<void> => {
    return new Promise((resolve, reject) => {
        
    })
}